/**
 * AOJ 提出時
 * ・このライブラリを入れて単一ファイルにする（AtCoder で使っているライブラリは使わないため取り除く（ファイルサイズが大きくて通らない））
 * e.g.
 * cargo equip --bin [bin_name] --remove docs --minify libs --exclude-atcoder-crates --exclude easy-ext ac-library-rs | pbcopy
 */
// pub mod geometry;
// pub mod graph;
// pub mod number;
// pub mod query;
// pub mod structure;
pub mod utils;
